import { ReservationDetail } from './reservation-detail.model';

describe('ReservationDetail', () => {
  it('should create an instance', () => {
    expect(new ReservationDetail()).toBeTruthy();
  });
});
